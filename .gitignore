# Compiled Python bytecode — regenerated on every run, never worth reviewing.
__pycache__/
*.py[cod]

# Local editor / OS noise
.DS_Store
Thumbs.db

# Runtime session artifacts — written by the app, one file per run.
data/sessions/
data/logs/*.pre_sessions.csv
*.tmp
